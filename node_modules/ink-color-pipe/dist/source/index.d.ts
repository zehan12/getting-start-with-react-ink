import { FunctionComponent, ReactNode } from 'react';
export declare type ColorProps = {
    styles?: string;
    children?: ReactNode;
};
declare const Color: FunctionComponent<ColorProps>;
export default Color;
