import React from 'react';
import { Transform } from 'ink';
import chalkPipe from 'chalk-pipe';
const Color = ({ styles = '', children }) => {
    return React.createElement(Transform, { transform: chalkPipe(styles) }, children);
};
export default Color;
